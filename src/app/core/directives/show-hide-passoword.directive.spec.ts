import { ShowHidePassowordDirective } from './show-hide-passoword.directive';

describe('ShowHidePassowordDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowHidePassowordDirective();
    expect(directive).toBeTruthy();
  });
});
